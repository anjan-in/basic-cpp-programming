//w.a.p to add,sub & multiply two numbers.
#include<iostream.h>
#include<conio.h>
void main()
{
       int  x,y,sum,diff,prod;
       clrscr();
       cout<<"Enter 2 nos:";
       cin>>x>>y;
       sum=x+y;
       diff=x-y;
       prod=x*y;
       cout<<"\nSum:"<<sum;
       cout<<"\nDifference:"<<diff;
       cout<<"\nProduct:"<<prod;
}